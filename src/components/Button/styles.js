import styled from 'styled-components';
import { azureRadiance } from '../../shared/ui-colors';

export const StyledButton = styled.button`
  border: ${azureRadiance} 1px solid;
  border-radius: 5px;
  padding: 8.5px 12px;
  color: ${azureRadiance};
  background: #fff;
`;
